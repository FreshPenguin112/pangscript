// Postinstall helper: attempt to install lightweight deps locally if missing.
// This is a no-op placeholder so `npm install` doesn't fail in environments without network.
try {
  // noop
} catch (e) {}
